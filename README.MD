# Byzantine Fault Tolerant Sharded Transaction Processing System

## Steps to run
- Modify `nodes.json` to reflect your set up
- Transactions as input in `transactions.csv`. Transactions arranged set-wise.
- `python3 starter.py` to start nodes
- `go run ./gateway/gateway.go` to start client and see menu
